<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Политекс — учёт продаж перчаток</title>
    <link rel="stylesheet" href="/static/css/style.css">
</head>
<body>

<h1>Политекс — учёт продаж строительных перчаток</h1>

<div class="block">
    <h2>Каталог товаров</h2>
    <table>
        <tr>
            <th>Название</th>
            <th>Цена</th>
            <th>Остаток</th>
        </tr>
        {% for g in gloves %}
        <tr>
            <td>{{ g[1] }}</td>
            <td>{{ g[2] }} ₽</td>
            <td>{{ g[3] }}</td>
        </tr>
        {% endfor %}
    </table>
</div>

<div class="block">
    <h2>Продать товар</h2>
    <form method="POST" action="/sell">
        <select name="glove_id">
            {% for g in gloves %}
            <option value="{{ g[0] }}">{{ g[1] }}</option>
            {% endfor %}
        </select>

        <input type="number" name="quantity" min="1" required>
        <button type="submit">Продать</button>
    </form>
</div>

<div class="block">
    <h2>История продаж</h2>
    <table>
        <tr>
            <th>ID</th>
            <th>Товар</th>
            <th>Количество</th>
            <th>Дата</th>
        </tr>
        {% for s in sales %}
        <tr>
            <td>{{ s[0] }}</td>
            <td>{{ s[1] }}</td>
            <td>{{ s[2] }}</td>
            <td>{{ s[3] }}</td>
        </tr>
        {% endfor %}
    </table>
</div>

</body>
</html>
